<footer id="footer" class="text-center text-lg-start" >
    <section class="border-top">
        <div class="container text-md-center mt-5">
            <div class="row mt-3">
                <div class="col-md-3 col-lg-4 col-xl-3 mx-auto mb-4">
                    <h6 class="text-uppercase fw-bold mb-4">
                        <i class="fa fa-gem me-3"></i>{{ "app.name"|trans }}
                    </h6>
                    <p>{{ "footer.project_description" |trans }}</p>
                </div>

                <div class="col-md-2 col-lg-2 col-xl-2 mx-auto mb-4">
                    <h6 class="text-uppercase fw-bold mb-4">
                        {{ "footer.about_project"|trans }}
                    </h6>
                    <p>
                        <a href="{{ path("how-it-works") }}"
                           class="text-reset">{{ "footer.about_how_it_works"|trans }}</a>
                    </p>
                    <p>
                        <a href="{{ path("inquiring") }}" class="text-reset">{{ "footer.about_inquiring"|trans }}</a>
                    </p>
                    <p>
                        <a href="{{ path("suppliers") }}" class="text-reset">{{ "footer.about_suppliers"|trans }}</a>
                    </p>
                </div>

                <div class="col-md-3 col-lg-2 col-xl-2 mx-auto mb-4">
                    <!-- Links -->
                    <h6 class="text-uppercase fw-bold mb-4">{{ "footer.useful_links"|trans }}</h6>
                    <p>
                        <a href="{{ path("terms-and-conditions") }}"
                           class="text-reset">{{ "footer.terms_and_conditions"|trans }}</a>
                    </p>
                    <p>
                        <a href="{{ path("app_register") }}" class="text-reset">{{ "footer.registration"|trans }}</a>
                    </p>
                </div>

                <div class="col-md-4 col-lg-3 col-xl-3 mx-auto mb-md-0 mb-4">
                    <!-- Links -->
                    <h6 class="text-uppercase fw-bold mb-4">
                        {{ "footer.contact"|trans }}
                    </h6>
                    <p>
                        <span class="fa fa-user me-1"></span>
                        {{ "app.contact_name"|trans }}
                    </p>
                    <p>
                        <span class="fa fa-envelope me-1"></span>
                        {{ "app.email"|trans }}
                    </p>
                    <!--  <p><i class="fa fa-phone me-3"></i> + 01 234 567 88</p>-->
                </div>
            </div>
        </div>
    </section>

    <div class="text-center p-4" style="background-color: rgba(0, 0, 0, 0.05);">
        &copy; 2021 {{ 'app.name'|trans }}
        <!-- <a class="text-reset fw-bold" href="#">Jaroslav Fikar</a>-->
    </div>
</footer>