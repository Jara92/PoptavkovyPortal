<nav class="navbar navbar-expand-lg navbar-light fixed-top py-3" data-navbar-on-scroll="data-navbar-on-scroll">
    <div class="container">
        {# Logo #}
        <a class="navbar-brand" href="{{ path("home") }}">
            <img src="{{ asset("build/libs/laslesvpn_template/img/icons/logo.png") }}" alt="" width="30"/>
            <span class="text-1000 fs-1 ms-2 fw-medium"><span class="fw-bold">{{ "app.name"|trans }}</span></span>
        </a>

        {# Mobile menu toggler #}
        <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span>
        </button>

        {# Menu items #}
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mx-auto border-bottom border-lg-bottom-0 pt-2 pt-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path("inquiries") }}">
                        {{ "mainnav.inquiries"|trans }}</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="{{ path("inquiring") }}">{{ "mainnav.inquiring"|trans }}</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="{{ path("suppliers") }}">{{ "mainnav.suppliers"|trans }}</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="{{ path("how-it-works") }}">{{ "mainnav.about_project"|trans }}</a>
                </li>
            </ul>

            <section class="d-flex py-3 py-lg-0">
                {% if app.user %}
                    <a class="btn btn-link text-1000 fw-medium order-1 order-lg-0" href="{{ path('app_logout') }}">
                        {{ "auth.logout"|trans }}</a>

                    <!--
            <a href="#" class="nav-link"></a>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown"
                   aria-haspopup="true"
                   aria-expanded="false">JMÉNO UŽIVATELE</a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item" href="#!">Moje poptávky</a>
                    <a class="dropdown-item" href="#!">Můj
                        profil</a>
                    @if($user->GetIsAdmin())
                    <a class="dropdown-item" href="#!">Administrace</a>
                    @endif
                    <a class="dropdown-item" href="{{ path('app_logout') }}">{{ "auth.logout"|trans }}</a>
                </div>
            </li>
            -->
                {% else %}
                    <a class="btn btn-link text-1000 fw-medium order-1 order-lg-0" href="{{ path('app_login') }}">
                        {{ "auth.login"|trans }}</a>
                {% endif %}

                <a class="btn btn-outline-danger rounded-pill order-0" href="{{ path("inquiries/new") }}">
                    {{ "inquiries.btn_create_inquiry"|trans }}</a>
            </section>
        </div>
    </div>
</nav>